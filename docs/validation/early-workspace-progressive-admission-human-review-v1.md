# Early Workspace / Progressive Admission — Focused Human Review Preparation

Status: implementation and focused browser probe completed; Human acceptance and closure pending. No production Work or PWU was executed in this validation.

## Focused evidence

- Node Control Room/state tests: 53 passed, 0 failed. Early projection tests cover BUILD versus direct question, current-step blocker, and existing exact-basis admission call.
- WIC progressive/unit contracts: 34 passed, 0 failed (Windows Python UTF-8 mode). Non-blocking reversible questions can remain in the assessment while Work Admission readiness has no current-step blocker; Human-owned questions still block.
- Isolated PostgreSQL integration: the new reversible-detail admission case passed. It verified zero Works before the Human admission operation, rejected empty Human authority, then checked the governed revision's source assessment and preserved unknown filename. The initially failing controlled-response test was reproduced. Its fake capability claimed only `provider_call_count=1` without identifying that call as the coalesced semantic-and-conversation request. The existing governed realizer correctly treats this as semantic-only evidence and invokes the response realizer. The fixture now supplies the coalesced provenance required by the existing contract; the exact test passes, with one simulated interpretation request, no response-realizer request, and two HTTP projection reads that add no Provider call or Turn. A separate semantic-only evidence contract remains covered. No product Provider behavior or allowed retry count changed.
- Browser probe on a disposable runtime: a simple HTML BUILD message received a real DeepSeek interpretation. The prospective four-surface Workspace and `按当前理解开始` appeared after interpretation and survived page reload; Reality reported no bound source, Production remained IDLE, and the Work count remained zero. Measured Reality/Agenda at y=-61, Production/Actions at y=243, with equal panel widths (423 px in this viewport). A separate direct question about HTML `table` did not activate the prospective Workspace. The disposable runtime persisted two test Interactions, zero Works, zero execution Attempts, zero Provider Reports; it was stopped with volumes preserved. Neither admission nor production was triggered.
- Final isolated Human runtime `watt-early-workspace-human-acceptance`: app, PostgreSQL, Coordinator, Native Worker and Tool Host up; `/health` and `/app` return 200; Alembic current/head `20260917_42`; zero Works/Interactions/Attempts/Provider Reports. DeepSeek WIC/Conversation/native-worker bindings are configured. URL: `http://127.0.0.1:8051/app`.

## V1.1 bounded revalidation

- The exact controlled-response test and semantic-only evidence contract pass. Four selected PostgreSQL admission/Steering tests and two selected controlled-stream tests pass. Repeated HTTP reads of the same Interaction are now asserted to leave the simulated Provider count at one and create no new Turn.
- Node Control Room/state: 53/53 pass. Runtime-activation unit contracts pass. The full focused Python UI-contract file passes after three stale text/source-shape expectations were aligned with the already accepted V4.1 composer and V4 asset revision; no UI product behavior changed.
- Compile/import, `uv lock --check`, `git diff --check`, and Alembic current/head pass. Full regression and Human acceptance were not run. The post-admission managed-workspace/Executor/Candidate end-to-end readiness claim remains **unproven** while truthful activation is blocked.

## Important limitation

The dirty review image and web mount are not a reviewed/committed Trusted Baseline. Initial Engineering Resource baseline revision is `93c7151fbf0648c908103d52a8c12581dea5f582`; the read-only Runtime activation projection is `ACTIVATION_BLOCKED` because all five active application evidence fields are absent. The 8051 override calls the ordinary repository/migration/bootstrap functions but omits `prepare_local_runtime_activation` and `_activated_environment`. The clean 8001 runtime uses the normal startup and reports `ACTIVE_AT_TRUSTED_BASELINE` for this same baseline. Simply adding the omitted startup calls to 8051 is **not** a truthful repair: its installed backend and read-only Web overlay contain uncommitted review changes, while the existing activation preparer fingerprints only the clean Engineering Resource checkout at HEAD. The tracked acceptance startup likewise overlays current source after fingerprinting HEAD; it does not register a distinct review-build identity or prove the served source matches the reported active version. No existing explicit review/test Runtime Version registration was found. Therefore no active-version evidence was fabricated and no trusted-baseline pointer, Runtime Commit, or Verification was changed. The browser probe proves pre-admission experience, not governed production, Candidate, Preview, Delivery or final trust. The Human's course-table admission/production journey remains pending. The 8051 environment remains clean (zero Works, Turns, Attempts, Provider Reports and queue items) but is **not** declared ready for the full Human production retest. A separately authorized truthful review-build activation/version seam or a clean admitted checkpoint is required; this V1.1 task does not supply one.

## Later Human Review Runtime Version repair (pre-acceptance)

The separately authorized Review Runtime Version slice resolves the above
*historical V1.1 blocker* without declaring the dirty application trusted.
The explicit review startup preserves the Engineering Resource's initial
Trusted Baseline, registers a content-identified Watt application version in
the isolated Runtime volume, and pins its ID in the application process.
The actual 8051 API now projects `ACTIVE_HUMAN_REVIEW`,
`activation_mode=HUMAN_REVIEW`, `active_application_revision=null`, and review
version `6d06137f84e21ac074f08c4c8b0e0cdb165fd331eba04e1004134a0059f7ccd8`.
Normal clean 8001 continues to project `ACTIVE_AT_TRUSTED_BASELINE` at the
committed revision. Modifying the mounted review Web source while the app was
running made the old review identity return `ACTIVATION_BLOCKED`; restarting
the app registered the new identity above. The manifest is retained under
`/var/lib/spg/human-review-versions/<version-id>.json`; the earlier review
identity is not overwritten. No trust or Work facts were forged.

Focused proof: Review identity mismatch and regeneration, source/package/static,
lock/config mismatch, normal-mode isolation, Work Baseline independence,
governed admission in both normal and review activation projections, managed
workspace delivery and Candidate preview, automatic orchestration, WIC
progressive sufficiency, and V4.1 UI contracts. Node Control Room/state tests
passed 54/54. `/health` and `/app` both returned 200; PostgreSQL current/head
remained `20260917_42`. The final Human Review product database has zero Works,
Interactions, Turns, Attempts, Provider Reports, and queue items. System review
version evidence exists outside product tables. The Human's course-table
journey, real Provider production, full regression and closure remain pending.

## V1.2 immediate engagement and progression repair

Status: focused implementation and qualification PASS; Human acceptance and
closure remain pending. Full regression was intentionally not run.

The first Send now reveals a presentation-only Workspace before interpretation
finishes. Reality, Agenda, Production, and Actions explicitly say that no Work,
PWU, repository, or production fact exists. A confirmed production Motive
latches the Workspace; later questions, failed Turns, polling, composer changes,
and completed replies cannot release it. A direct question can retract a
provisional shell after classification and creates no Work. Browser qualification
with the real DeepSeek review binding observed the course-table shell while the
Watt reply was still processing, then observed the retained shell and
`按当前理解开始`. Refresh preserved the same prospective Workspace. A separate
arithmetic direct question showed the shell only while interpretation was
pending and returned to the blank interaction surface after the answer. The
probe also found and fixed a transient duplicate Human-message rendering path;
Current Interaction remained above the Composer (`bottom=738`, Composer
`top=747` in the qualification viewport).

The historical PREPARING Work was inspected without mutation. Work
`377afc36-ae1d-54bc-8b7d-b940fd5cb0e1`, Reality Revision
`948b4cac-e248-5f8f-bd6c-9af0f80793fa`, and Interaction
`cc0dbcad-e847-4279-a29d-dbaa92685363` had completed six of seven Design steps.
Step 7 (`ea966c95-e92d-42c7-991c-70a127e855fc`) had no persisted result because
the DeepSeek semantic response omitted required field `disposition`. The driver
truthfully stopped before a Production Run, PWU, Attempt, Dispatch, Queue Entry,
Allocation, Executor binding, Runtime activation, or managed workspace existed.
The exact first missing transition was therefore Step 7 semantic-result
admission, before DESIGN -> PRODUCE. The semantic adapter now allows exactly one
strict regeneration only for that single missing-field defect. A bounded,
reversible CODE_WORK proposal targeting a Watt-managed repository can then be
admitted under the already recorded Work authority; external repositories,
broad targets, later production cycles, and material Human decisions still stop
for Human review. Focused PostgreSQL proof reaches `PRODUCTION_RUNNING`, creates
one Production Run and one PWU, schedules the Work, and creates no ceremonial
proposal-review Attention.

The two historical Provider failures remain evidence. Turn
`1f1f1250-ff4c-4fd0-8a0d-b36369e85524` failed in coalesced interpretation with
`root:json_invalid`; the
old path performed no repair and did not persist a Provider request ID or safe
raw excerpt. Turn `30d7a9b3-8a8e-47c8-9c00-5b513117d480` reached conversation
realization but rejected
the Human-facing result; validated structured semantics were unnecessarily lost.
The observed-stream coalesced path now permits one syntax-only JSON repair and
never retries schema, semantic, or authority failures. Conversation realization
now falls back deterministically to claims already present in validated
structured semantics, so wording failure cannot mutate or discard Work truth.
Focused controlled-provider tests cover repair success, one-attempt failure,
non-retryable schema failure, and realization fallback. Current diagnostics log
only request/model/stage/shape/length/validation facts, never credentials or raw
secret-bearing content.

Active-Work operational questions such as `目前执行到什么状态了？` now use a
read-only projection assembled from current Work Reality, Steering, Runtime,
Attention, and execution facts. The focused PostgreSQL test uses a Provider that
would fail if invoked and proves no Work Reality Revision, Steering decision,
Production Run, or PWU is created. Ordinary side questions remain conversational;
unrelated production Motives retain the existing New Work boundary.

The clean final project is `watt-early-workspace-v12-human-review`. App,
PostgreSQL, Coordinator, Native Worker, Tool Host, scheduler/queue, and DeepSeek
WIC/Conversation/native execution bindings are running. `/health` and `/app`
return 200; Alembic current/head is `20260917_42`; Runtime state is
`ACTIVE_HUMAN_REVIEW`, not a trusted/release claim. Exact Human Review Runtime
Version is
`96198a68d438a2cebf58c1ddb16d3f30cf0878d052250a587ae63b39e076464e`.
Its product database starts with zero Works, Interactions, Turns, Production
Runs, PWUs, Attempts, queue entries, and Provider Reports. URL:
`http://127.0.0.1:8053/app`.

Focused verification: 90 of 96 selected Python WIC/Provider/PostgreSQL tests
passed and six explicitly marked real-Provider tests were skipped by the test
harness. After the final operational-question matcher change, an exact 11-case
gate passed,
including all four required operational-question forms, both bounded Provider
repair paths, read-only non-mutation, managed proposal progression, and the
existing Watt-managed production-to-delivery path. Node Control Room/state tests
passed 55/55. Python compile/import, JavaScript syntax, `uv lock --check`,
`git diff --check`, Alembic current/head, HTTP health, and the clean-runtime data
check passed. Two bounded browser calls supplied the real DeepSeek UI evidence;
the Human's final acceptance journey was not executed.

## Main Journey stability qualification

Status: autonomous qualification PASS; a new clean Human Review Runtime is
prepared separately below. Human acceptance, full regression, closure, commit,
and push remain intentionally pending.

The disposable project `watt-main-journey-qualification-4` exercised the real
browser and real configured Provider path from an empty product database. The
first course-table Send opened the provisional four-surface Workspace before
the response completed, while the API still reported zero Works. Reality said
that no engineering source existed, Production was truthfully IDLE, and Actions
said no authority transfer or production had occurred. Structured
interpretation retained the Workspace and exposed the single
`按当前理解开始` action. Human admission then created Work
`0646cba4-dda0-5b2e-af50-366aefe2fcb9`; Steering resolved all seven scoped
design obligations, allocated a Watt-managed repository, admitted the bounded
`index.html` proposal under the existing Work authority, and progressed to
Produce without a second proposal-review ceremony or manual Advance.

The native execution lineage was:

- Production Run `0637f70d-807b-4f63-8a1a-a8451ed02406`;
- PWU `606c4f83-357d-4228-87a8-1bedbd5dad2a`;
- Attempt `bbb43357-5282-42a5-b829-a3a920799b6c`;
- Queue Entry `cfc31483-1502-450b-b9c0-fb66a013bf56`, `COMPLETED`;
- Allocation `90bebe68-6235-4987-b0e5-7f58d7e8f805`, released by
  `native-worker-main-journey-qualification`;
- Dispatch `8e4c5928-adee-49c6-84da-598584870708`;
- terminal native outcome `RESULT_READY`;
- sealed Candidate `52bdce4d-b574-5c58-a371-4f5e679f23d9`.

Independent verification recorded `PATH_SCOPE: PASS` and
`GIT_DIFF_CHECK: PASS`. The Candidate produced only `index.html`. Its exact
preview returned HTTP 200, declared UTF-8, contained the table, and referenced
no external HTTP resource. At this boundary the Work projected
`NEEDS_ATTENTION`, `next_owner=HUMAN`, one
`CANDIDATE_AUTHORIZATION` Attention with exactly `AUTHORIZE`, and no control
state violation. No Candidate authorization or repository integration was
performed.

The real browser then asked `目前执行到什么状态了？`. Turn
`24ae5c80-c367-42e2-9db5-922732ef3379` completed through
`watt-native:work-reality-query`, answered that the exact Candidate awaited
Human review, and left the Workspace visible. Before and after the query, Work
Reality Revision `d488499c-67a7-5126-ad20-ff83c8dbf4a5`, Steering revision
count 1, PWU count 1, Attempt count 1, and queue-entry count 1 were unchanged.
The query therefore created no Work mutation, Steering mutation, PWU, retry, or
production side effect.

The initial browser probe also exposed a 160 ms visual overlap while a local
optimistic Human message yielded to the persisted message. Identical
replacement nodes now leave immediately, while ordinary Current Interaction
handoff animation remains. The refreshed UI and the Work Reality query showed
one Human bubble and one Watt bubble, with no duplicate.

Provider robustness remains bounded: only `root:json_invalid` receives one
strict repair; schema, semantic, and authority failures do not enter retry
loops. If Human-facing realization fails after semantic validation, the
deterministic fallback uses only admitted semantic claims and cannot mutate
Work truth. Focused Provider tests cover repair success, repair exhaustion,
non-retryable failures, and realization fallback.

Focused validation after the final changes:

- native Executor contracts in the Linux qualification image: 46/46 PASS;
- Node Control Room/state contracts: 55/55 PASS;
- selected WIC/Provider/control-state contracts: 17/17 PASS;
- selected PostgreSQL main-flow, managed-workspace, Candidate, and read-only
  query contracts: 4/4 PASS;
- real browser/Provider main journey: PASS to sealed Candidate;
- exact preview and non-mutation checks: PASS.

Pause/Resume, Stop, and Cancel were qualified earlier in the disposable
`watt-main-journey-qualification-3` runtime using real process execution and
process-tree termination receipts. Pause checkpointed and resumed the same
Attempt to `RESULT_READY`; Stop ended `STOPPED`; Cancel ended `CANCELLED`.
Those qualification-only attempts deliberately remain outside the clean main
journey and final Human Review databases.

The final clean project is `watt-main-journey-human-review`. App, PostgreSQL,
Coordinator, Native Worker, Tool Host, scheduler/queue, and DeepSeek
WIC/Conversation/native execution bindings are running at
`http://127.0.0.1:8053/app`. `/health` and `/app` return HTTP 200; Alembic
current/head is `20260917_42`. Runtime mode is `HUMAN_REVIEW`, state is
`ACTIVE_HUMAN_REVIEW`, and exact review version is
`00859b04958c89122764bbeef32d1f4c3eaee128573d17075d4b21f985974476`.
The clean database contains zero Works, Interactions, Turns, Production Runs,
PWUs, Attempts, queue entries, Provider Reports, and Runtime Commits. No
qualification or Human-acceptance scenario has been executed in it.
