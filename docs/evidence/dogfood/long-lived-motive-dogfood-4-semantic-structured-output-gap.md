# Long-lived Motive Dogfood #4 — Structured Semantic Output Finding

## Preserved Runtime Reality

Dogfood #4 reached the real semantic boundary from the same long-lived Motive:

```text
Work Admission                     PASS
Steering Activation                PASS
Real Semantic Provider             PASS
Structured Semantic Result         FAIL
Provider Threads / Turns            1 / 1
Provider terminal status            completed
JSON decoding                       PASS
SemanticStepResultCandidate         absent
Governed SemanticStepResult         absent
Current Step                        DESIGN
Production facts                    0
Driver                              STOPPED / BLOCKED
```

The returned JSON used `BOUNDED_CODE_CHANGE` instead of the admitted production
target kinds, plain strings instead of typed artifact-target objects, and
natural-language descriptions instead of bounded repository areas ending in
`/**`. Strict payload validation correctly failed closed before candidate
construction or governed persistence.

The preserved `watt-motive-dogfood-4` Runtime remains immutable evidence and is
not repaired, retried, or rewritten by MVP-PLAN-STEER-1J.

## Classification

```text
SEMANTIC_STRUCTURED_OUTPUT_GAP
    RESOLVED IN IMPLEMENTATION / FOCUSED VALIDATION

SEMANTIC_EXECUTION_OBSERVABILITY_GAP
    CONFIRMED / PARTIALLY MITIGATED
    NOT YET CLOSED
```

The main Work projection continued to imply active semantic evaluation after
the authoritative Driver projection had become `STOPPED / BLOCKED`.

## Admitted Correction

MVP-PLAN-STEER-1J supplies the JSON schema generated from the existing typed
semantic payload contract to the Provider Turn. It does not add aliases,
coercions, guessed paths, or a permissive repair parser. Existing JSON/Pydantic,
basis, staleness, Resource, Scope, evidence, authority, completion, and result
admission checks remain mandatory.

The Work API also reuses the existing Driver state to expose a truthful
stopped/blocked event and next action without redesigning the UI.

## Current Status

```text
MVP-PLAN-STEER-1J
    CLOSED / PASS
    ARCHITECTURE LEAD REALITY REVIEW = PASS

Reality-driven Plan Steering
    IMPLEMENTED FOR MVP BEHAVIOR
    REAL PROVIDER DOGFOOD CONTINUES
```

Core Closure is not claimed by this evidence.
