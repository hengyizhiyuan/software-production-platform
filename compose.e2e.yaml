# Compose with compose.native-executor.yaml and the provider profile.
# This profile uses the same API-key-only Watt Native Executor chain.
services:
  app:
    environment:
      SPG_RUNTIME_PROFILE: local-docker-native-e2e
      SPG_VERIFICATION_ADAPTER: contract-driven-repository
